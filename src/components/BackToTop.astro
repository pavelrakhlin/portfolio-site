<button
  type="button"
  class="back-to-top"
  data-back-to-top
  hidden
  aria-hidden="true"
>
  <svg
    class="back-to-top__icon"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    stroke-width="2"
    stroke-linecap="round"
    stroke-linejoin="round"
    aria-hidden="true"
  >
    <path d="M12 19V5"></path>
    <path d="m5 12 7-7 7 7"></path>
  </svg>
  Back to top
</button>

<style>
  .back-to-top {
    position: fixed;
    /* Horizontal position set in JS from `#main` bounds. */
    bottom: var(--space-6);
    z-index: 70;
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-4);
    border: none;
    border-radius: 9999px;
    background: #1a1a1a;
    color: #ffffff;
    font: inherit;
    font-size: var(--text-sm);
    line-height: 1.4;
    cursor: pointer;
    box-shadow: 0 4px 20px rgb(0 0 0 / 0.18);
    opacity: 0;
    transform: translateX(-50%) translateY(8px);
    transition: opacity 0.2s var(--ease), transform 0.2s var(--ease);
  }

  .back-to-top:hover {
    opacity: 0.92;
  }

  .back-to-top:focus-visible {
    outline: 2px solid #ffffff;
    outline-offset: 2px;
  }

  .back-to-top__icon {
    width: 1rem;
    height: 1rem;
    flex-shrink: 0;
  }

  @media (max-width: 1024px) {
    .back-to-top {
      bottom: var(--space-4);
    }
  }
</style>
